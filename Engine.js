import { StyleSheet, Text, View } from "react-native";
import React from "react";

const w = (props) => {
  console.log(props)
  return (
    <View>
      <Text>w</Text>
    </View>
  );
};

export default w;

const styles = StyleSheet.create({});
